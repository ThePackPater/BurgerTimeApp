<li>
	{{name}}

	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}DEVOUR{{else}}REMAKE{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">End this Burger!</button>
</li>